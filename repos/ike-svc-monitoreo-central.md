# ike-svc-monitoreo-central

Librería Maven `observability-starter` (MDC, Feign, logs JSON, Micrometer). **No es un servicio HTTP.** No hay contrato, roles ni env de runtime de app.

Publica a GitHub Packages en push a `main`. No hay deploy a EKS.
